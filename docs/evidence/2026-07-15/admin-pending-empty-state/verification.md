# 管理员待审核空状态验收

日期：2026-07-15

## 诊断结论

- 使用服务端 trusted Supabase 客户端执行只读查询，没有写入或修改远端数据。
- 远端共有 18 条收藏；4 条 `review_requested=true` 收藏全部已有审核结果，其中 3 条 `changes_requested`、1 条 `adopted`。
- 按产品状态机，已有审核结果的收藏不属于“待管理员审核”；因此当前真实 pending 数为 0。
- 当前新建的品牌“11”收藏为用户自写，但 `review_requested=false`，不会进入管理员队列。
- 原服务端 anti-join 返回 0 是正确结果；诊断期间尝试的错误过滤改动已完全撤回。

## 修复范围

- 管理员筛选按钮显示 `只看待审核（N）`。
- pending=0 时显示“当前没有待管理员审核的文案”。
- 补充说明：已通过或需用户修改的文案在“全部收藏”查看；用户修改并重新提交后才再次进入待审队列。
- 不改变审核状态、分组隔离、查询接口或数据库结构。

## 自动化验证

- 新增管理员空队列行为测试：1/1。
- 完整客户端：34 文件，389/389。
- 服务端受影响回归：3 文件，25/25。
- Client production build 通过。
- `/admin` 与 `/api/health` 本地 HTTP 均返回 200。

## 手工测试右下角提醒

1. 在 `/app` 打开收藏库，找到品牌“11”的自写收藏。
2. 勾选“提交管理员审核”，等待同步完成后切换到 `/admin`。
3. 管理员页重新聚焦或刷新后，右上角应显示待审数字，右下角出现“N 条文案待审核”。
4. 点击“稍后审核”，同一批次再次聚焦不重复弹出。
5. 再新增一条待审收藏，或让用户修改后重新提交，切回管理员页应再次提醒。

## 边界

- 未创建、修改或删除远端 QA 数据。
- 无 Migration、部署、Git commit/push、reset/clean 或 Worktree。
