# Codex 独立复验 Prompt — Playwright 运行时基线返工

日期：2026-07-15
执行方：Grok Build
项目：`D:\work\77港话通社媒文案\77`
分支：`master`
HEAD：`92358e297cfba8ce11cba7889e8d98ae25ea2117`

## 结论（给 Codex）

**本切片结果是 BLOCKED，不是 COMPLETE。**

- 本机**没有**可用 Node 22；仅 `v26.1.0` @ `C:\Program Files\nodejs\node.exe`。
- nvm / fnm / volta / nvs 均不存在。
- Grok **未安装** Node、npm 依赖或 Playwright 浏览器。
- Grok **未**在本切片对 focused E2E 声称通过；**未**跑 `npm run verify`。
- 上一轮 “public smoke COMPLETE” 已在状态/证据中**作废**。

请独立验收「阻塞声明是否诚实 + 无风险整理是否正确」，**不要**因为文档写了 blocked 就去要求业务功能继续开发。

## 起始 / 结束 git 状态

复验时请自行再跑 `git status --short`。Grok 预期至少包含：

```text
# 上一轮保留的 dirty + 本切片无风险改动（示例，以你本机实时 status 为准）
 M .planning/context_pack.md
 M .planning/loop_log.md
 M .planning/progress.md
 M .planning/status.md
 M .gitignore
 M package.json
 M e2e/smoke.spec.ts
 M scripts/verify/commands.md
 M spec/ACCEPTANCE.md
 M spec/CHANGELOG.md
 M spec/TEST_PLAN.md
 D playwright.config.ts
?? .nvmrc
?? .node-version
?? .planning/prompts/20260715-182800-codex-review.md
?? .planning/prompts/20260715-190000-grok-playwright-runtime-repair.md
?? .planning/prompts/20260715-180200-codex-review.md
?? .planning/prompts/20260715-grok-playwright-runner-public-smoke.md
?? docs/evidence/2026-07-15/playwright-runtime-repair/
?? docs/evidence/2026-07-15/playwright-runner-public-smoke/
?? docs/handoff/2026-07-15-grok-playwright-e2e-handoff.md
?? docs/handoff/2026-07-15-grok-playwright-runtime-repair-handoff.md
?? e2e/protected-route.spec.ts
?? e2e/public-routes.spec.ts
?? playwright.config.mjs
```

未 commit / 未 push。保留用户全部 dirty worktree。

## 本切片改动文件清单

| 文件 | 意图 |
| --- | --- |
| `.nvmrc` | 记录 Node 22 |
| `.node-version` | 记录 Node 22 |
| `package.json` | `engines.node: 22.x`（声明，非自动切换） |
| `.gitignore` | 忽略 `test-results/` 等 |
| `.planning/status.md` | COMPLETE → **BLOCKED** |
| `.planning/progress.md` / `loop_log.md` / `context_pack.md` | 同步阻塞 |
| `spec/TEST_PLAN.md` / `ACCEPTANCE.md` / `CHANGELOG.md` | 返工结论 + 去尾随空格 |
| `scripts/verify/commands.md` | 强调 Node 22 前置 |
| `docs/evidence/2026-07-15/playwright-runtime-repair/**` | 阻塞证据 |
| 本文件 | Codex 复验 prompt |

## Node 事实

| 项 | 值 |
| --- | --- |
| 实际 Node | `v26.1.0` |
| 路径 | `C:\Program Files\nodejs\node.exe` |
| Node 22 是否存在 | **否** |
| 是否执行安装 | **否** |

## 命令结果（Grok）

| 命令 | 结果 |
| --- | --- |
| `node --version` | v26.1.0 |
| version managers | all missing |
| `npx playwright --version` | 1.61.1 |
| `playwright test --list --config=playwright.config.mjs` | OK，10 tests，exit 0 |
| focused E2E ×2 on Node 22 | **未执行**（无 Node 22） |
| focused E2E on Node 26 this session | **故意未跑**（避免重复挂起/残留进程；沿用 Codex 挂起事实） |
| `npm run verify` | **未执行** |
| 本任务残留 Playwright PID | **无**（未启动 execute） |

## Codex 最小复验命令

```powershell
cd D:\work\77港话通社媒文案\77
git rev-parse HEAD
git status --short
node --version
where.exe node
Get-Command nvm, fnm, volta -ErrorAction SilentlyContinue
git diff --check
npx playwright test --list --config=playwright.config.mjs
```

若 **仍无 Node 22**：

- 验收本切片为 **blocked 正确** 即可。
- **不要**要求 Grok 伪造 Node 22 下的通过。
- 向用户确认是否授权安装 Node 22 LTS。

若 **已有 Node 22**（用户安装后）：

```powershell
node --version   # 必须 v22.x
npx playwright test e2e/smoke.spec.ts e2e/public-routes.spec.ts e2e/protected-route.spec.ts --config=playwright.config.mjs --project=chromium --reporter=list
# 再跑一遍同一 focused 命令
# 检查本任务 Playwright 进程是否已退出
```

两次都必须有 list reporter 实际输出、全通过、正常退出，才可打开后续切片。

## 证据路径

1. `docs/evidence/2026-07-15/playwright-runtime-repair/verification.md`
2. `docs/evidence/2026-07-15/playwright-runtime-repair/test-output.txt`
3. `docs/evidence/2026-07-15/playwright-runtime-repair/screenshots/`（空 — 无通过 E2E）
4. 本 prompt：`.planning/prompts/20260715-182800-codex-review.md`

## 明确声明

- 未安装 Node / 浏览器 / npm 依赖
- 未部署、未 migration、未真实远程写入
- 未 commit / push
- 未伪造测试成功
- 未进入真实登录 / 工作台 / 支付开发

## 给用户的下一步

1. 授权安装 Node 22 LTS，或提供现有 Node 22 路径。
2. 安装后再跑 runtime-repair 验收；通过后才规划隔离工作台 mock smoke。
