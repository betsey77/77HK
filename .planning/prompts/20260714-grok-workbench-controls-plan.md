你是增量开发规划者，只读，不得修改文件。

项目：D:\work\77港话通社媒文案\77。

先完整阅读 README.md、AGENTS.md、CLAUDE.md、spec/WORKBENCH_CONTENT_CONTROLS.md、spec/PRD.md、spec/SDD.md、spec/TEST_PLAN.md 和当前相关代码。

用户已授权后续实现 W1-W4 与左侧折叠页：

1. 文案类型、默认关闭的五档长度控制、主语气加最多两个修饰语气。
2. 保存配置、云同步、历史恢复、收藏快照。
3. 每用户正反例案例库；案例 title 为选填、body 和 reason 必填。
4. Prompt 正反例注入必须覆盖 DeepSeek、CantoneseLLM、rules，保留既有参考收藏案例和节日注入。
5. 收藏与管理员显示文案类型；普通管理员可受审计地查看及单条复制用户收藏生成正文；超级管理员额外受审计地查看及单条复制案例正文，二者只读。
6. 按规格第 8 节实现左侧五个折叠分组，且不改变已有视觉、路由、支付或功能。

硬边界：

- 不删除或重构无关功能。
- 不创建平行项目。
- 不读取、打印或修改 .env 或任何秘密。
- 不推送远端 Supabase Migration，不部署，不进行真实支付。
- 不使用子代理。
- 保留端口、路由、登录、支付、额度、RLS 等既有能力。

请输出：

A. 现有实现与规格的差异。
B. 精确文件清单，区分需改、需新建、不改。
C. 分 W1-W4 加折叠页的小切片实施顺序，以及每个切片的测试。
D. Migration、RLS、审计风险。
E. 哪些文件需要在写入前做逐文件备份，以支持不丢失当前脏工作区的回退。

不要写任何代码或文件。
