# Codex 独立复验 Prompt — E2E harness hardening

日期：2026-07-15
执行：Grok Build
项目：`D:\work\77港话通社媒文案\77`
HEAD：`92358e297cfba8ce11cba7889e8d98ae25ea2117`（dirty worktree，未 commit）

## 结论

本切片完成 Windows E2E ASCII 镜像工具的 **fail-closed junction** 与 **仓库 evidence 截图回写**。
Grok 本地：`-SelfTest` PASS；`-Twice` 两次 **8 passed**；截图在仓库 evidence 目录；无 Playwright CLI 残留。

## 精确改动

| 文件 | 改动 |
| --- | --- |
| `scripts/e2e-public-smoke.ps1` | fail-closed junction；marker；Node 22 强制；5173 检查；`E2E_SCREENSHOT_DIR`；写 `test-output.txt`；`-SelfTest`；移除 `-InstallBrowsers` |
| `e2e/public-routes.spec.ts` | 支持 `E2E_SCREENSHOT_DIR`，默认保持历史相对路径 |
| planning / spec / commands | 状态与验收说明 |
| `docs/evidence/2026-07-15/e2e-harness-hardening/**` | verification、test-output、screenshots |

未改 `client/`、`server/` 业务实现。

## Node

- 版本：`v22.23.1`
- 路径：`C:\Users\35308\AppData\Local\nodejs-versions\node-v22.23.1-win-x64\node.exe`
- 未安装新 Node/浏览器/npm 包

## 两次 E2E

| Run | 结果 |
| --- | --- |
| RUN1 | 8 passed ~11.4s exit 0 |
| RUN2 | 8 passed ~11.3s exit 0 |
| 残留 CLI | 无 |

## 截图路径（仓库内）

`docs/evidence/2026-07-15/e2e-harness-hardening/screenshots/`
（home/pricing/login desktop + mobile 共 5 张）

## 安全拒绝测试

`powershell -File scripts/e2e-public-smoke.ps1 -SelfTest`

- 普通目录 → REFUSE，不删除
- 目标错误的 junction → REFUSE，不删除
- 正确 junction → 仅移除链接，目标保留
- 无 marker 且无已证明 junction 的已有根目录 → REFUSE

## 证据

- `docs/evidence/2026-07-15/e2e-harness-hardening/verification.md`
- `docs/evidence/2026-07-15/e2e-harness-hardening/test-output.txt`
- `docs/evidence/2026-07-15/e2e-harness-hardening/screenshots/`

## Codex 最小复验

```powershell
cd D:\work\77港话通社媒文案\77
git rev-parse HEAD
git status --short

# 1) fail-closed
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\e2e-public-smoke.ps1 -SelfTest

# 2) 确保 http://localhost:5173 可达后
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\e2e-public-smoke.ps1 -Twice

# 3) 截图必须在仓库内本切片目录
dir docs\evidence\2026-07-15\e2e-harness-hardening\screenshots

git diff --check
```

通过标准：SelfTest PASS；两次 8/8；截图在上述仓库路径；无本任务 Playwright 残留；未 commit。

## 明确未做

真实 Auth / RLS / 支付 / 部署 / migration / commit / push / 安装依赖
