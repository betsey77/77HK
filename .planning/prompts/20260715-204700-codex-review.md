# Codex 独立复验 Prompt — Playwright 运行时基线（Node 22 + ASCII cwd）

日期：2026-07-15
执行方：Grok Build
项目：`D:\work\77港话通社媒文案\77`
分支：`master`
HEAD：`92358e297cfba8ce11cba7889e8d98ae25ea2117`

## 结论（给 Codex）

本切片在 **Node v22.23.1** 下完成 focused public/protected E2E **连续两次 8/8**，有 list reporter 输出并正常退出。

**关键约束：** 不要在含中文的仓库根路径直接 `npx playwright test`（本机会无输出挂起）。请用：

```powershell
npm run test:e2e:smoke:win:twice
# 或 scripts/e2e-public-smoke.ps1 -Twice
```

该脚本在 `C:\work\77hk-e2e`（ASCII）运行，junction 到 monorepo 的 `node_modules` / `client` / `server`。

## Node 事实

| 项 | 值 |
| --- | --- |
| 验收 Node | **v22.23.1** |
| 路径 | `C:\Users\35308\AppData\Local\nodejs-versions\node-v22.23.1-win-x64\node.exe` |
| 安装方式 | 用户授权后下载官方 win-x64 zip 便携安装（未卸载系统 Node 26） |
| 系统默认 | 仍可能是 v26.1.0 @ `C:\Program Files\nodejs\node.exe` |
| 是否 npm install / playwright install | **否**（浏览器缓存已有） |

## 根因摘要

- 项目路径含非 ASCII（`77港话通…`）时，Playwright **执行阶段** worker 挂起、无 reporter。
- `--list` 正常；`playwright` 库直接 `chromium.launch` 正常。
- Node 26 vs 22 **不是** 挂起的充分条件；ASCII cwd + Node 22 为可重复基线。

## 两次 focused 结果（Grok）

| Run | 结果 |
| --- | --- |
| RUN1 | 8 passed ~10.8s exit 0 |
| RUN2 | 8 passed ~10.8s exit 0 |
| 残留 CLI | 无 |

证据：

- `docs/evidence/2026-07-15/playwright-runtime-repair/verification.md`
- `docs/evidence/2026-07-15/playwright-runtime-repair/run1-node22.txt`
- `docs/evidence/2026-07-15/playwright-runtime-repair/run2-node22.txt`
- `docs/evidence/2026-07-15/playwright-runtime-repair/test-output.txt`
- `docs/evidence/2026-07-15/playwright-runtime-repair/screenshots/`

## 改动文件（主要）

- `scripts/e2e-public-smoke.ps1`（新增）
- `package.json`（engines + `test:e2e:smoke:win` / `:twice`）
- `playwright.config.mjs`（路径说明）
- `.nvmrc` / `.node-version` / `.gitignore`
- `spec/*`、`.planning/*`、evidence、本文件

未 commit / 未 push。

## Codex 最小复验

```powershell
cd D:\work\77港话通社媒文案\77
git rev-parse HEAD
git status --short
& "$env:LOCALAPPDATA\nodejs-versions\node-v22.23.1-win-x64\node.exe" --version
# 确保 http://localhost:5173 可达，或去掉脚本默认 E2E_NO_WEBSERVER 让 webServer 启动
npm run test:e2e:smoke:win:twice
git diff --check
```

通过标准：两次 focused 均有 reporter 行、8 passed、正常退出、无本任务残留 Playwright CLI。

## 明确声明

- 已按用户授权安装 Node 22 便携版；未部署、未 migration、未真实远程写、未 commit/push
- 未伪造通过
- 未进入真实登录 / 工作台 / 支付开发
